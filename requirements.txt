# ============================================================
# requirements.txt (ROOT) - untuk menjalankan NOTEBOOK + script EVALUASI
# ============================================================
# Backend dan frontend punya requirements sendiri (dipakai saat build Docker):
#   backend/requirements.txt   -> FastAPI + model (torch, transformers, chromadb)
#   frontend/requirements.txt  -> Streamlit + requests (ringan)
# File ini untuk pipeline training di notebooks/ dan evaluasi di evaluation/.
# ============================================================

# Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0

# Visualisasi (EDA / figures)
matplotlib>=3.8.0

# NLP - training sentiment & classifier (IndoBERT)
transformers>=4.40.0
torch>=2.0.0
datasets>=2.18.0
scikit-learn>=1.3.0
accelerate>=0.27.0

# Embedding & Vector DB
sentence-transformers>=2.7.0
chromadb>=0.4.24

# LLM clients (untuk batch_run / evaluasi). Pakai salah satu / dua-duanya.
groq>=0.5.0
google-genai>=0.3.0          # SDK baru: dipakai lewat `from google import genai`

# Evaluasi opsional (jalur --ragas di evaluate_logbook.py)
# ragas>=0.1.0
# langchain-google-genai>=1.0.0

# Utility
python-dotenv>=1.0.0