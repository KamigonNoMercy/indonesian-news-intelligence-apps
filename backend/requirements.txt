# ============================================================
# Dependensi BACKEND (API) - tanpa Streamlit, torch CPU-only
# ============================================================
# Catatan torch CPU-only (image Docker jadi kecil): saat build Docker, install
# torch dari index CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Di lokal Windows kamu yang udah ada torch GPU, gak perlu diubah.

# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# --- NLP / model ---
transformers>=4.40.0
torch>=2.0.0
sentence-transformers>=2.7.0

# --- Vector DB ---
chromadb>=0.4.24

# --- LLM clients ---
groq>=0.5.0
google-genai>=0.3.0

# --- Utility ---
python-dotenv>=1.0.0
